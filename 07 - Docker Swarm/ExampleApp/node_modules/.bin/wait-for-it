#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/bash" ]; then
  "$basedir/bash"  "$basedir/../wait-for-it.sh/bin/wait-for-it" "$@"
  ret=$?
else 
  bash  "$basedir/../wait-for-it.sh/bin/wait-for-it" "$@"
  ret=$?
fi
exit $ret
